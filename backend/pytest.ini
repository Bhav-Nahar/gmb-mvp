[pytest]
# Only collect the real test suites. Excludes manual scratch scripts at the repo
# root (e.g. run_groq_test.py) that define a function named `test` and hit live
# services, which pytest would otherwise try to collect.
testpaths = tests
